<section class="user-section">
  <h2 class="d-flex"><%= "Name: #{@user.name}" %><%= invitation_handler(@user) %></h2>
  <article class="timeline">
    <h3>Recent posts:</h3>
    <ul class="posts">
      <%= render @posts %>
    </ul>
  </article>
</section>
